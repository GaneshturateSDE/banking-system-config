spring:
  application:
    name: Auth-service
  datasource:
    url: jdbc:postgreSql://localhost:5432/auth-db
    username: ganesh
    password: ganesh
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:/db/migration

server:
  port: 9000



